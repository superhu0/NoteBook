1、封装：通过函数将一定的功能步骤涵盖起来，外部可通过调用函数实现封装的功能。
2、面向对象封装：通过在类内部构建函数（方法）的方式，将实例的数据保护起来，即只允许外部通过调用内部函数（方法）来处理数据，实现数据的保护。
3、python类中数据保护方式：'__foo'两个下划线，私有变量，只能该类内部自己调用，子类及外部环境无法调用；'_foo'一个下划线，受保护的变量，不建议外部调用。ps：
'__foo__'前后各有两个下划线，python类中自带的特殊方法。
